# Force Swagger UI to route cleanly to /swagger-ui.html
springdoc.swagger-ui.path=/swagger-ui.html

# Define where the underlying OpenAPI JSON metadata contract lives
springdoc.api-docs.path=/v3/api-docs

